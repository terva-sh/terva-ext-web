// Package version holds the extension's version string, shared by the
// protocol hello and the default User-Agent.
package version

// Version is the zot-web release version.
const Version = "0.2.0"
