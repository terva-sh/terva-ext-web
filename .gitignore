# build output (binary name matches extension.json "exec")
/terva-ext-web
/terva-ext-web.exe
/bin/

# local config / secrets that may sit next to the binary during dev
config.json

# editor / OS noise
*.swp
.DS_Store
dist/
/.release-package/

/validation-report-*.json
